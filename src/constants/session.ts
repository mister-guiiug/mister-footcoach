/**
 * The currently signed-in user for this local MVP.
 *
 * The real app will resolve this from Clerk/Convex auth (see
 * docs/conception-technique.md §8). Until the backend adapter is wired in,
 * the app runs as the coach of team "U13 A".
 */
export const CURRENT_USER_ID = 'u1';
